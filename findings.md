# Findings & Decisions

## Requirements
- 基于 `D:\Stellara Work` 文件和程序设计产品网站。
- 主导航必须有：首页、文档、下载。
- 整体视觉简洁、克制，明确避免科幻风。
- 交互性强，同时保持清晰、易用和响应式。

## Research Findings
- 当前网站工作区最初为空，可作为新站点初始化。
- 源程序目录包含 `README.md`、`docs`、`assets`、`src`、`electron`、`release` 和版本配置，足以提取真实产品信息。
- 产品名为 Stellara Work，当前版本为 `0.9.0` / “v0.9 内测版”，定位是“数据本地的 Codex 风格 Windows 桌面 Agent”。
- 当前程序基于 Electron、React 与 TypeScript，仅提供 Windows x64 的 NSIS 安装包命名规则 `Stellara Work-Setup-0.9.0.exe`。
- 核心卖点包括本地数据、危险操作审批、API Key 主进程隔离，以及 GLM / DeepSeek / Kimi / MiniMax / 自定义 OpenAI 兼容模型支持。
- 源 README 的“W1/W2/W3”状态可能已落后于实际界面代码；后续以现有 UI、测试和发布产物共同判断。
- Sites 模板已等价复制到工作区，并保留规划文件；模板包含 vinext、Next.js、Cloudflare Workers 兼容构建与 `.openai/hosting.json`。
- 依赖安装在工具的 60 秒窗口内超时，但已落盘 385 个顶层模块且没有残留 npm 进程；需要用依赖树或构建进一步确认完整性。
- 在允许访问系统 npm 缓存后，`npm ci` 已成功安装 507 个包，`npm ls --depth=0` 通过。
- vinext 开发服务器已在 `http://localhost:3000/` 启动并在 Codex 中打开；日志中的 `Request.cf` 回退只影响本地 Cloudflare 请求上下文，不阻塞页面开发。
- 发布目录确实包含 `Stellara Work-Setup-0.9.0.exe`、blockmap 和完整的 win-unpacked 产物，因此下载页可以基于真实安装包状态呈现。
- 现有 React UI 已包含侧栏、主视图、工作区面板、模型卡片、命令面板、审批顶部栏、Diff/Shell/工具结果卡、文件树、设置和新手引导等成熟功能模块。
- 源项目有两套近期设计文档：会话与设置设计、2026-07-27 UI 重设计；可据此提炼准确交互与产品语言。
- 可复用官方产品图标包含 16–512px PNG、ICO 和 JPG，网站将采用产品自身图标，不另造品牌图形。
- 现有 UI 设计规范明确以 Linear / Raycast 级专业工具为审美锚点，强调“工具而非玩具”，并明确远离 AI / 科幻视觉；网站应延续这一产品性格。
- 应用现有设计以浅色为主、深炭侧栏和克制蓝色强调，使用系统无衬线与等宽代码字体、4/8px 间距节奏和小圆角；网站可在更宽松的营销排版中复用这些品牌线索。
- 真实交互包括项目与会话分组、搜索、重命名/导出、命令面板、可配置快捷键、审批顶部条、工作区进度、文件树和模型切换；首页演示应优先呈现这些差异化能力。
- 安装包为 `Stellara Work-Setup-0.9.0.exe`，大小 117,214,681 字节（111.8 MiB），生成于 2026-07-30 14:37:38。
- 安装包 SHA-256 为 `78DBC0D14441E1FE98164C88BC5A57027BE126DD5EF0C00C5AC636F7C1580037`。
- NSIS 配置明确不限制 Windows 版本，构建架构为 x64；下载页应只陈述“Windows x64”，不虚构最低系统版本。
- 首次 UI/UX 数据库匹配误判为 OLED 深色、科幻等宽字体与发光效果，已因直接违反用户要求而否决；重新以“浅色、编辑化、瑞士极简、专业工具”检索后得到与产品规范一致的方向。
- 最终设计系统：浅色为主、Slate 中性色 + 克制蓝色、系统/Inter 风格无衬线、充分留白、清晰网格、轻阴影和 200–250ms 状态过渡；保留 44px 触控目标、明显焦点和 reduced-motion 支持。

## Technical Decisions
| Decision | Rationale |
|----------|-----------|
| 网站使用 Sites 的多路由能力路径 | 需要首页、文档、下载三个独立路由 |
| 真实内容优先 | 页面文案、功能和版本信息从源程序提取，避免空泛模板化内容 |
| CSS/组件图形优先 | 无需额外图片即可形成简洁、可信的产品视觉 |
| 下载页提供真实发布产物信息 | 安装包名称和系统要求可从 package.json 与 release 目录确认 |
| 拒绝 UI/UX 检索的首个深色科幻方案 | 用户约束和源程序设计规范的优先级高于自动推荐 |
| 以浅色瑞士极简为最终视觉系统 | 与用户要求、产品现有 Linear/Raycast 工具感以及文档网站可读性一致 |

## Issues Encountered
| Issue | Resolution |
|-------|------------|
| 上一轮执行中断 | 重新核对目标目录为空、源目录完整，再继续执行 |
| Sites 初始化器要求目标目录绝对为空，与先创建规划文件的要求冲突 | 读取初始化脚本后复制同一模板内容，并保留任务规划文件 |
| `npm ci` 在 60 秒后被工具终止 | 检查已安装模块，再用依赖校验/构建确认；缺失时采用不同安装方式补齐 |
| 后台 `npm install` 因沙箱无法写系统缓存而触发 npm 内部错误 | 改为获批的沙箱外 `npm ci`，安装成功 |

## Resources
- 源程序：`D:\Stellara Work`
- 网站工作区：`D:\Stellara Work网站`

## Visual/Browser Findings
- 产品图标是浅灰圆角方形底，中心为抽象的 `S/W` 组合：左半为深灰蓝，右半为明亮品牌蓝。形态简洁、几何化，适合在导航、下载卡片和浏览器图标中直接复用。
- 站点初始预览是临时加载骨架，尚未包含产品界面；实现时必须完整替换并移除 `_sites-preview`。
- 社交预览图生成结果为浅色瑞士网格构图，左侧保留官方图标、品牌名和正确的中文标题“把复杂工作，留在本地完成。”，右侧是克制的工作台界面抽象；无科幻、霓虹或多余文案，已通过文字与风格检查并保存为 `public/og.png`。

## Detailed Documentation Upgrade
- 文档页应采用“内容优先的知识库”信息架构：突出全局搜索、主题分类、章节目录、相关章节与未解决问题的后续路径。
- 文档专用设计检索继续推荐 Slate 中性色、品牌蓝、Inter/系统无衬线和高可读浅色表面，与现有网站方向一致。
- 检索结果中的超大夸张标题不适合高密度技术文档，继续沿用克制标题比例，并把视觉层级主要交给间距、边框、标签和目录状态。
- 必须保留清晰焦点、键盘可操作搜索、44px 触控目标、移动端无横向滚动和 reduced-motion 支持。
- 当前没有独立的 docs 页面设计覆盖文件；全局设计系统实际位于 `design-system/stellara-work-website/MASTER.md`。
- 现有文档只有 7 篇文章，数据模型主要是单段正文、步骤、代码和提示，无法清楚表达参数表、检查清单、警告、键值说明、故障现象/原因/解决方案等详细信息。
- 当前搜索只过滤左侧文章列表，没有结果数量、命中关键词范围或自动切换到首个结果；当当前文章不在结果中时，正文仍显示旧文章，容易产生“搜索无效”的错觉。
- 当前桌面三栏结构（主题目录、正文、本文目录）适合继续扩展；应优化为更高效的主题分组、文章元信息、章节内锚点与上一篇/下一篇，而不是堆叠超长单页。
- 现有代码块具备复制反馈，搜索具备 `/` 聚焦和 `Esc` 清空，是值得保留并增强的交互基础。
- 源 README 的 W1/W2/W3 进度文字明显落后于当前代码；详细文档继续以实际组件、配置实现、测试和安装包为准，不把旧进度表当作现状。
- 当前设置中心实际包含 5 个页签：模型、会话、应用、快捷键、Skills；模型页支持添加、连接测试、更新 Key、上下文窗口、设为活跃和删除。
- 添加模型时只有连接测试成功才写入配置；失败提示明确要求检查 API Key、Base URL 与网络，可纳入故障排查。
- 快捷键规范除已有的命令面板、侧栏、工作区、发送和设置外，还包括计划模式、工作区模式、标签页切换/关闭/恢复；所有绑定可录制和恢复默认。
- 删除模型会同时移除对应密钥，但不会删除历史会话；历史会话引用已删除模型时应提示用户重新选择模型。
- 以 `shared/shortcuts.ts` 为准，当前实际默认快捷键没有“Ctrl+, 打开设置”；现有网站文档这一项不准确，应删除。实际绑定包括 Ctrl+B、Ctrl+Shift+W、Ctrl+Shift+P、Ctrl+Enter、Escape、Ctrl+K、Ctrl+1–9、Ctrl+W、Ctrl+Shift+T。
- 模型上下文窗口可选 256K、512K、1M，默认 256K；默认上下文压缩阈值是窗口的 90%。
- 模型配置包含预设/自定义、API Key、Base URL、模型名、工作目录、上下文窗口和活跃状态；内置预设的 Base URL/模型名自动填入，自定义模型需要手动填写。
- 应用设置包括紧凑侧栏/Tab 栏、默认工作目录、数据目录入口、主日志入口、深色/浅色/跟随系统主题、复制诊断信息，以及输入 DELETE 才能执行的“清空所有数据”。
- 清空全部数据的实际范围是 `config.json`、`.env` 和 `stellara.db`，执行后需要重启应用。
- 左侧栏会按项目分组展示会话，并为未归属项目的会话提供“未分组”区域；搜索按会话标题过滤并自动展开命中的项目组。
- 项目支持新建、重命名、在项目中新建会话和删除；删除项目不会删除其会话，而是把这些会话移到“未分组”。
- 会话支持打开、内联重命名、删除与导出 JSON；删除会话会级联删除该会话消息，关闭标签页只关闭当前视图，不删除本地会话。
- 已关闭标签页可以通过 Ctrl+Shift+T 恢复；会话记录包含模型、工作目录、所属项目、消息数量与更新时间，并按最近更新排序。
- 数据库会持续保存会话与消息；没有新增消息时的普通保存不会无意义地改变会话排序。
- 快捷键设置支持逐项“录制”和“重置”：进入录制后按组合键写入，Esc 取消；每项可单独恢复默认值。
- Skills 从当前工作目录的 `skills/*.json` 自动加载，每个文件必须包含 `name`、`description`、`prompt`；设置页可重新加载、打开目录和展开查看 prompt，聊天框使用 `/skill-name` 调用并提供输入补全。
- 命令面板不仅用于跳转，还可打开设置/Skills/文件树、切换工作目录、新建/切换/删除会话、切换模型、切换主题、显隐左右侧栏以及切换 Plan 模式；面板支持搜索、上下键选择、Enter 执行和 Esc 关闭。
- 敏感工具调用会显示固定顶部审批栏，明确展示工具名与参数，并只提供“拒绝”和“允许这一次”；文档不能暗示永久授权。
- Plan 模式使用只读工具集，命令面板描述也明确为“只读分析，不修改文件，先输出执行计划”。
- Shell 工具采用无 shell 执行、命令白名单、单行限制与工作目录路径约束；拒绝管道/重定向/变量展开、绝对路径、越界路径和破坏性文件命令，输出上限为 5 MiB。
- 首次配置需要选择工作目录，并完成模型连接测试后才能保存；内置 Provider 与自定义 OpenAI 兼容端点使用同一条“测试后保存”流程。
- 工作区检查器有目标、进度、交付物、文件四区：普通任务按工具操作计进度，Plan 任务按步骤计进度；计划步骤可手动循环标记待做/完成/失败，交付物来自本次会话的写入与编辑文件。
- Plan 文本会解析编号步骤及 `READY TO EXECUTE` 标记，执行阶段会维护 pending/in_progress/completed；修改文件后会提示重读验证，常见命令错误也会生成针对性修复引导。
- 当前可分类的模型错误包括 API Key 无效、429 限流、余额/配额、模型不存在、上下文过长、请求格式、Provider 5xx、网络、120 秒流空闲超时和用户取消；UI 会按错误类型提供打开设置、切换模型、重试或检查网络指引。
- `~/.stellara/.env` 在 v0.9 中是权限为 0600 的本地明文密钥文件；裸 API Key 仅供 Electron 主进程使用，IPC/诊断信息只暴露“是否已配置”，不回传密钥值。
- Electron 窗口启用 `nodeIntegration: false`、`contextIsolation: true`、`sandbox: true`、`webSecurity: true`，因此文档可准确说明渲染界面与 Node 能力隔离。
- “复制诊断信息”会包含版本、平台、架构、应用设置、模型脱敏配置和会话概况，不包含 API Key，适合纳入故障反馈流程。
- 4 个内置模型预设的当前精确标识为 GLM-5.2、DeepSeek-v4-Pro、Kimi-K3、MiniMax-M3，另有一个自定义 OpenAI 兼容槽位；内置预设自动填 Base URL 和 model，自定义槽位要求手工填写。
- Build 模式工具包括文件读取/写入/精确编辑、命令、文件名搜索、内容搜索、目录树、HTTP GET、任务完成、Git 状态/diff/log，以及记忆搜索/保存；只读集合是 read/search/list 与 Git 查看操作。
- 实际需要审批的工具集合是 `write_file`、`edit_file`、`run_command`、`web_fetch`；审批默认 60 秒超时并默认拒绝，允许范围被钳制在 1–300 秒。
- `read_file` 单文件上限 10 MiB，支持 1-based 的 offset/limit 行范围；`edit_file` 默认要求 oldText 唯一精确匹配，`replaceAll=true` 才会替换全部。
- 清空所有数据的实现删除整个 `~/.stellara` 后重新初始化空数据库；诊断字段实际是版本、平台、架构、Node/Electron 版本、模型数、会话数、活跃模型 ID、是否有工作目录和日志/数据路径，不包含会话正文或密钥。

## Frosted Navigation Upgrade
- 用户截图明确指向全站顶部导航栏，而不是页面正文或文档侧栏。
- 磨砂效果应保持文字和按钮对比度：使用高透明度白色表面、`backdrop-filter` 模糊/轻饱和、可见底边和极轻阴影。
- 需要同时提供 `-webkit-backdrop-filter`，并让不支持背景模糊的浏览器仍显示可读的半透明白色背景。
- 移动端展开菜单应沿用同一磨砂表面，避免顶部栏磨砂但下拉菜单恢复纯白造成视觉断层。
- UI/UX 检索给出的 teal/orange 配色与现有品牌不一致，因此只采用其“轻微交互、清晰对比、快速响应”规则，颜色继续以现有 Slate + 品牌蓝主设计系统为准。
- 现有 `.site-header` 已有 `rgba(255,255,255,.94)` 和 `blur(14px)`，但透明度过高，实际视觉接近纯白；同时缺少 `-webkit-backdrop-filter`，移动端 `.mobile-nav` 仍是纯白背景。
- 本次应把头部表面调整为约 0.76–0.82 的白色透明度、18–20px 模糊和轻度饱和，并用 `@supports` 为支持背景模糊的浏览器提供更透明表面；不支持时维持高不透明回退。
- 最终实现采用 0.78 白色透明度、20px blur、155% saturate、半透明底边和极轻阴影；不支持背景模糊时回退为 0.96 白色。
- 移动端菜单按钮与展开层同步采用半透明表面，桌面/移动端保持统一，同时保留 44px 触控目标和现有焦点样式。
- 提交 `37b1d33` 已在 GitHub Pages 工作流 `30709283444` 部署成功；公开 CSS 实测包含标准及 WebKit 磨砂属性，三条路由均为 HTTP 200。
- 同一提交已保存为 Sites v2 并完成私有生产部署；安装包没有上传，也未进入发布归档。

## Documentation Reading Progress Fix
- 用户截图显示文档页面已滚动到最底部，但顶部蓝色阅读进度条仍明显低于 100%，说明当前算法没有以浏览器实际可滚动距离作为完成边界。
- 修复应优先使用 `scrollHeight - clientHeight` 作为分母，并在距离底部只剩极小浮点误差时钳制为 100%；同时要处理短页面（无可滚动距离）和窗口尺寸变化。
- 进度更新属于高频滚动交互，应避免重复布局读写或不必要的 React 状态更新，必要时用 `requestAnimationFrame` 合并更新。
- 根因已确认：现有代码用“已滚动距离 ÷ 文章完整高度”计算，但文章完整高度中包含当前视口已显示、无需继续滚动的部分，因此页面到达最大滚动位置时分子天然小于分母。
- 当前监听只有 `scroll`，窗口尺寸变化后不会立即重算；实现应同时监听 `resize`，并以文章开始位置、文章底部可达位置和页面最大滚动位置共同确定区间。
- 进度值需要统一钳制到 0–1，并在距离页面底部约 1–2px 时明确返回 1，以吸收浏览器缩放和小数像素误差。
- UI/UX 检索确认长文档适合保留明确的阅读进度提示，但它推荐的“夸张极简/超大标题”不适用于当前高密度技术文档，因此不改变视觉体系，只修正进度行为。
- 项目没有文档页独立 override，继续以 `design-system/stellara-work-website/MASTER.md` 的 Slate + 品牌蓝、克制动效和可访问性规则为准。
- 现有进度条已具备 `role="progressbar"`、数值范围和 `aria-valuenow`；修复计算后这些语义会自动同步，无需增加新的视觉元素。
- 当前测试覆盖服务端路由和文档内容，但没有覆盖客户端阅读进度算法；本次应把计算抽成纯函数，增加顶部、过程、页面底部、短页面和浮点容差用例，防止回归。
- 项目使用 Node 22.19 与 TypeScript 5.9，可优先采用项目现有工具链执行纯逻辑测试，避免为了一个边界计算新增测试依赖。
- 最终算法不再使用文章完整高度直接做分母，而是计算从 `articleTop - stickyOffset` 到 `min(articleBottom - viewportHeight, maxPageScroll)` 的可达区间；这使 100% 与用户真正能到达的位置一致。
- 监听 resize 可覆盖浏览器缩放、横竖屏切换和窗口变化；`requestAnimationFrame` 合并高频滚动更新，符合进度指示器的性能要求。
- 第一轮验证结果：5 个进度边界测试、ESLint、TypeScript 与 `git diff --check` 全部通过。
- 完整生产构建与 9 项回归测试通过；Pages 静态导出生成全部三条路由，文档交互包包含新版进度逻辑，且发布目录仍不含安装包。
- 最终常规生产构建已在静态导出检查后重新生成，确保后续 Sites 归档包含正确的 server 入口。
- 发布提交为 `817fa52`，四个提交文件分别是进度组件、纯计算函数、回归测试和默认测试脚本；内部规划记录、`IDEA.md` 与安装包均未提交。
- Sites v3 和 GitHub Pages 工作流 `30710005149` 均已成功发布提交 `817fa52`；下一步只需核对公开资源已刷新。
- 公开文档已加载 `DocsExplorer-DZrfADNo.js`，资源返回 HTTP 200，并包含 `requestAnimationFrame`、resize、`scrollHeight`、`clientHeight` 等新版逻辑；首页、文档、下载三条路由均返回 200。

## Seamless Header Transition
- 用户截图中的“这条”指向全宽顶部导航栏底部可见的灰色横线；结合上一轮磨砂实现，最可能来源是 `.site-header` 的半透明底边与线性阴影。
- “无缝显示”应理解为让导航栏与下方页面连续融合，而不是移除当前导航项的蓝色活动下划线。
- 修改范围应仅限头部表面的分隔边与阴影，继续保留半透明白色、背景模糊、饱和度和不支持模糊时的可读回退。
- CSS 核对确认接缝由 `border-bottom: 1px solid var(--header-glass-border)` 和 `--header-glass-shadow` 中的线性/扩散阴影共同产生；不是页面主体或导航活动态造成。
- 为避免 1px 高度变化引发布局轻微跳动，底边应保留为 1px 透明边框，只把颜色设为 transparent；阴影应设为 none，磨砂本身即可提供层次。
- 移动端展开菜单内部的 `border-top` 用于区分菜单层级，不属于截图中的全宽接缝，暂不移除。
- UI/UX 检索继续强调克制、少装饰和清晰导航；其夸张标题与新配色不适合现有站点，继续遵循既有 MASTER，仅采用“减少多余装饰”的原则。
- 实现已将头部底边 token 设为 transparent、阴影 token 设为 none；保留现有 CSS 结构和 1px 透明占位，因此不会改变头部高度或 sticky 偏移。
- 首轮验证结果：ESLint、TypeScript、常规生产构建、9 项内容/路由/进度回归测试及 `git diff --check` 全部通过。
- Pages 静态 CSS 已确认 `--header-glass-border: transparent`、`--header-glass-shadow: none`，同时仍包含 20px/155% backdrop-filter；三条静态路由完整且没有安装包。
- 静态检查后已重新生成常规 server 构建，确保 Sites 发布归档与最终源码一致。
- 发布提交为 `251e230`，仅修改两个头部玻璃 token；内部规划记录、`IDEA.md` 和安装包均未提交。
- Sites v4 与 GitHub Pages 工作流 `30710348093` 均已成功；首次公开资源读取遇到临时 EOF，站点实现无需修改，改用带重试的传输方式核对线上 CSS。
- 公开 CSS `index-DWN6lk07.css` 已确认 `--header-glass-border: transparent`、`--header-glass-shadow: none` 且保留 20px/155% backdrop-filter；三条公网路由均为 200。
