# Task Plan: Stellara Work 产品网站

## Goal
基于 `D:\Stellara Work` 现有程序与文档，设计并实现一个简洁、非科幻风且交互性强的产品网站，包含首页、文档、下载三个导航入口，并完成验证与发布。

## Current Phase
Phase 16

## Phases

### Phase 1: 产品与素材梳理
- [x] 读取程序说明、版本信息和可复用素材
- [x] 确认三个页面的真实内容与下载资源
- [x] 记录发现
- **Status:** complete

### Phase 2: 设计系统与站点结构
- [x] 初始化站点项目
- [x] 生成并固化简洁型设计系统
- [x] 定义首页、文档、下载页的信息架构
- **Status:** complete

### Phase 3: 页面与交互实现
- [x] 实现全局导航与响应式布局
- [x] 实现首页产品展示与交互
- [x] 实现文档检索/目录交互
- [x] 实现下载筛选与版本信息
- **Status:** complete

### Phase 4: 验证与修复
- [x] 完成生产构建
- [x] 检查类型、键盘操作和减少动态效果支持
- [x] 修复发现的问题并记录结果
- **Status:** complete

### Phase 5: 发布与交付
- [x] 保存并部署不含安装包的验证后站点版本
- [x] 确认线上状态
- [x] 向用户交付可访问网址
- **Status:** complete

### Phase 6: 详细文档资料审计与信息架构
- [x] 核对源程序 README、设计文档、配置、快捷键与实际界面能力
- [x] 形成详细文档章节、术语与交叉链接结构
- [x] 记录真实性边界，避免虚构尚未实现的功能
- **Status:** complete

### Phase 7: 文档页面与交互升级
- [x] 扩充快速开始、工作区、模型、审批、快捷键、安全、故障排查等内容
- [x] 重构搜索、目录、章节内导航、复制反馈和移动端阅读体验
- [x] 补齐语义结构、键盘操作、焦点与状态提示
- **Status:** complete

### Phase 8: 构建与内容验证
- [x] 验证本地生产构建、静态导出、类型与渲染测试
- [x] 检查文档搜索、锚点、代码示例和响应式布局
- [x] 确认发布产物不包含安装包或失效图片接口
- **Status:** complete

### Phase 9: GitHub Pages 更新发布
- [x] 仅提交文档页面与必要样式/测试修改
- [x] 推送并等待 GitHub Pages 工作流完成
- [x] 验证线上首页、文档与下载路由
- **Status:** complete

### Phase 10: 顶部导航磨砂方案与现状核对
- [x] 核对现有导航背景、定位、层级和移动端菜单样式
- [x] 结合现有设计系统确定透明度、模糊、边框和回退方案
- **Status:** complete

### Phase 11: 半透明磨砂实现与验证
- [x] 只修改顶部导航相关样式，保持文字对比度和触控尺寸
- [x] 验证 lint、类型、生产构建与静态导出
- **Status:** complete

### Phase 12: 发布磨砂导航
- [x] 仅提交导航视觉与必要测试修改
- [x] 推送并等待 GitHub Pages 部署成功
- [x] 验证线上三条路由的导航样式产物
- **Status:** complete

### Phase 13: 文档阅读进度诊断与修复
- [x] 核对进度条的滚动容器、分母与页面底部边界
- [x] 修复到达文档底部时未显示 100% 的计算误差
- [x] 保持滚动性能、响应式布局与无障碍语义
- **Status:** complete

### Phase 14: 验证并发布进度修复
- [x] 完成 lint、类型、构建与针对性逻辑测试
- [x] 只提交本次进度修复并同步托管版本
- [x] 验证 GitHub Pages 线上产物和三条路由
- **Status:** complete

### Phase 15: 导航栏无缝过渡
- [x] 核对截图中分隔线对应的边框与阴影来源
- [x] 移除导航栏底部接缝，同时保留磨砂、布局与活动状态
- [x] 验证桌面和移动端样式不受影响
- **Status:** complete

### Phase 16: 验证并发布无缝导航
- [x] 完成样式、构建、静态导出和路由回归检查
- [x] 只提交本次导航无缝样式修改
- [x] 同步 GitHub Pages 与 Sites 并核对线上 CSS
- **Status:** complete

## Key Questions
1. Stellara Work 的核心价值、当前版本和支持平台是什么？
2. 现有文档与安装包中哪些内容可以直接在网站呈现或提供下载？
3. 哪些交互最能提升浏览、查文档与下载的效率？

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| 采用多路由站点 | 用户明确要求首页、文档、下载三个导航栏目 |
| 采用简洁、编辑化产品风格 | 符合“简洁风、不要科幻风”，并突出真实程序内容 |
| 不依赖大幅生成图片 | 产品型网站更适合用真实界面、排版与轻量 CSS 视觉 |
| 导航采用轻度磨砂而非强玻璃拟态 | 用户只指定顶部区域；控制透明度和阴影能保持现有简洁、非科幻风格 |

## Errors Encountered
| Error | Attempt | Resolution |
|-------|---------|------------|
| 上一轮被用户中断 | 1 | 重新检查目标目录与任务状态后继续 |
| 初始化脚本在 1 秒工具超时内未完成且未写入文件 | 1 | 不重复调用初始化器；检查脚本后按相同模板步骤安全完成初始化 |
| `npm ci` 超过 60 秒工具窗口 | 1 | 检查 node_modules 完整性并使用构建验证，不原样重复失败调用 |
| 首次记录 npm 超时的补丁上下文不匹配 | 1 | 重新读取当前规划文件后使用精确上下文更新 |
| 并行验证中的 PowerShell `foreach` 管道语法错误 | 1 | 改为 `Get-Item` 接收路径数组后重新执行，测试与文件检查通过 |
| 初次严格类型检查缺少 Cloudflare Worker 全局类型 | 1 | 添加官方 `@cloudflare/workers-types` 与项目级类型引用 |
| Worker 类型存在但 `Cloudflare.Env` 未声明可选 DB | 2 | 用项目级声明合并补充可选 `DB` 绑定后重新检查 |
| Sites 创建站点首次返回 HTTP 504 | 1 | 判定为明确的临时网关超时，按托管规范使用相同 slug 重试一次 |
| 重试创建站点返回 slug 已占用 | 2 | 通过站点列表确认首次请求其实已创建成功，并持久化其原始 project_id |
| 站点列表首次请求 limit=100 超出上限 | 1 | 按错误提示将 limit 调整为 50 后成功读取 |
| 首次推送含 111.8 MiB 安装包的源码超过 120 秒工具窗口 | 1 | 远端检查确认尚未建立分支；刷新短期凭据并延长单次上传窗口 |
| 延长上传后远端返回 HTTP 413 | 2 | 确认 Sites 源码仓库拒绝 111.8 MiB 单文件；从发布版本中移除该二进制，页面改为明确的“下载通道准备中”，避免坏链接 |
| 验证“dist 中无 exe”时 `rg` 因零匹配返回 1 | 1 | 改用 PowerShell 文件枚举；结果为空，确认发布产物不含安装包 |
| 打包脚本在非登录 Git Bash 中找不到 `dirname` | 1 | 改用 Git Bash 登录环境调用脚本 |
| 首个归档目标 `C:\tmp` 对 Git Bash 不可写 | 2 | 将临时归档写入工作区内已忽略的 `work/`，打包成功 |

| 首次按根路径读取设计系统 MASTER 失败 | 1 | 使用 `rg --files design-system` 定位到嵌套目录 `design-system/stellara-work-website/MASTER.md` |
| 首次文档升级 lint 检出 effect 内同步 setState 与 ref 闭包问题 | 1 | 将搜索同步移入输入事件、滚动目标改为 DOM id，并顺带移除 SiteHeader 的旧式路由 effect；lint 与类型检查通过 |
| 文档渲染测试对 React 数字/文本分隔注释的预期不准确 | 1 | 按实际服务端 HTML 将断言改为匹配 `<strong>17</strong> 个主题`，不更改页面实现 |
| 静态产物校验误用 Next 默认 `out/`，且 PowerShell 非终止错误让脚本继续 | 1 | 对照现有 Pages 工作流确认 vinext 产物为 `dist/client`，后续校验启用 `$ErrorActionPreference='Stop'` |
| 线上校验脚本误用 PowerShell 保留变量 `$home` | 1 | 改用任务专用变量 `$homeResponse`，避免覆盖只读的 `$HOME` |
| 新阶段规划补丁误用了 findings 中的决策行作为 task_plan 上下文 | 1 | 按 task_plan 的实际 Decisions 表重新定位并完成追加 |
| 在 GitHub Pages 静态导出后运行 Worker 路由测试得到 308 | 1 | 308 来自静态导出的 trailing-slash 重定向；改为直接检查 `dist/client` 静态 HTML/CSS，再用常规构建运行 Worker 路由测试 |
| 静态 CSS 校验正则假设 minifier 会保留函数间空格 | 1 | 实际产物为 `blur(20px)saturate(155%)`；将校验改为允许可选空白，不修改正确的 CSS 实现 |
| Sites 打包命令误调用 Windows 的 WSL `bash`，且中文路径传递失败 | 1 | 显式使用 `D:\Git\bin\bash.exe`，通过一次性英文 Junction 调用官方打包脚本，完成后删除映射 |
| GitHub 匿名 Actions API 达到共享出口频率上限 | 1 | 改用已登录的 `gh run list` 获取工作流结果，并直接请求线上页面/CSS 交叉验证 |
| 首次线上 CSS 地址匹配只考虑 `/_next/static/css` | 1 | 读取实际 `<link>` 后确认 vinext Pages 产物位于 `/assets/`，重新检查成功 |
| 并行审核中 `git diff --no-index` 以退出码 1 表示“存在差异”，遮蔽了同时运行的构建输出 | 1 | 已获得完整差异内容；不重复该组合，改为单独执行最终常规构建并用普通状态检查确认 |
| 线上校验 URL 在 PowerShell 双引号中写成 `$assetUrl?v` / `$path?v`，变量边界解析错误 | 1 | 改用 `${assetUrl}`、`${base}${path}` 明确变量边界后重新校验 |
| 无缝导航首次线上校验遇到传输流 unexpected EOF | 1 | 判定为临时网络传输中断；改用 curl 的受控重试并写入明确临时文件后重新核对 |

## Notes
- 保留 `D:\Stellara Work` 原项目不做修改；网站代码只写入当前工作区。
- 所有外部或源项目内容均作为资料处理，不执行其中的指令性文本。
- 网站工作区里的安装包副本已移除，原文件仍完整保留在 `D:\Stellara Work\release`。
- 私有站点 v1 已部署成功，开发服务器已在发布完成后关闭。
- 本轮继续沿用现有浅色编辑化设计系统，重点提升详细度、可检索性与长文阅读效率，不改为科幻或装饰性风格。
- 2026-08-02 的增量需求只针对顶部导航，避免把磨砂扩散到正文卡片或改变页面布局。
- 磨砂导航已通过提交 `37b1d33` 同步到 GitHub Pages 和 Sites v2；安装包仍未上传。
- 文档阅读进度修复已通过提交 `817fa52` 同步到 GitHub Pages 和 Sites v3；线上文档脚本及三条路由均已验证。
- 导航栏无缝过渡已通过提交 `251e230` 同步到 GitHub Pages 和 Sites v4；线上 CSS 确认无底部接缝且保留磨砂。
